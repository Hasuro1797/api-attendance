export enum Roles {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum Modality {
  PRESENTIAL = 'PRESENTIAL',
  REMOTE = 'REMOTE',
}

export enum Status {
  PRESENT = 'PRESENT',
  ABSENT = 'ABSENT',
  LATE = 'LATE',
}
